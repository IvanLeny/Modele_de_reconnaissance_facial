# Système RAG hybride MINPMEESA — dépendances (chapitre 3.1)
#
# Socle minimal, 100% exécutable hors-ligne (aucun modèle à télécharger) :
pymupdf>=1.24          # extraction PDF (texte + blocs positionnés)
rank-bm25>=0.2.2       # index lexical BM25
scikit-learn>=1.3      # substitut vectoriel TF-IDF/LSA + utilitaires
numpy>=1.24            # calcul vectoriel
streamlit>=1.30        # interface web du prototype (chapitre 3.7)

# Configuration de référence (déploiement local souverain) — recommandée
# lorsque l'accès aux modèles pré-entraînés est disponible :
#   fastembed>=0.3               # encodeur de phrases multilingue (ONNX, CPU)
#   sentence-transformers>=2.2   # alternative PyTorch (encodeur + cross-encodeur)
#
# Le système sélectionne automatiquement le meilleur backend disponible et
# bascule sur le substitut hors-ligne si aucun modèle n'est accessible
# (voir rag_minpmeesa/index/embeddings.py).

# Tests
pytest>=7.0
